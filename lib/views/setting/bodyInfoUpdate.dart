import 'package:flutter/material.dart';

class bodyInfoUpdate extends StatelessWidget {
  const bodyInfoUpdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
